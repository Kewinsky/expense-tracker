export const dropdownData = (items) => {
  return items.map((item) => ({
    value: item,
    label: item,
  }));
};

export const dropdownDataNumber = (items) => {
  return items.map((item) => ({
    value: item,
    label: item,
  }));
};
